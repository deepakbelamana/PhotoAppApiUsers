#url on where the config server is working
spring.cloud.config.uri=http://localhost:8012

#if user-microservice.properties is present in config server, then the name should be user-microservice
#here we are simply mentioning the config server application name so it will fetch from
#default application properties
spring.cloud.config.name= PhotoAppApiConfigServer